<Page x:Class="SubtitleTools.UI.Pages.SynchronizeTimingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SubtitleTools.UI.Pages"
      xmlns:ctrl="clr-namespace:SubtitleTools.UI.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="280" d:DesignWidth="350"
      Title="Synchronize Timings">

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Grid Margin="8" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock Text="Sync Points" FontWeight="SemiBold" Margin="0,0,0,4"/>

            <ListView Grid.Row="1" MinWidth="300" Height="100" Margin="8,0,0,0">
                <ListView.View>
                    <GridView>
                        <GridView.Columns>
                            <GridViewColumn Header="Subtitle No." Width="80"/>
                            <GridViewColumn Header="Start" Width="80"/>
                            <GridViewColumn Header="New Start" Width="80"/>
                        </GridView.Columns>
                    </GridView>
                </ListView.View>
            </ListView>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="8,0,0,0">
                <Button Content="Add" Width="40" Margin="0,4,4,0"/>
                <Button Content="Remove" Width="60" Margin="0,4,4,0"/>
            </StackPanel>

            <TextBlock Grid.Row="3" Text="Apply To" FontWeight="SemiBold" Margin="0,8,0,4"/>
            <StackPanel Grid.Row="4" Grid.ColumnSpan="3" Margin="8,4,0,8" HorizontalAlignment="Left">
                <RadioButton Content="_All subtitles"/>
                <RadioButton Content="_Subtitles between sync points"/>
            </StackPanel>

            <Button Grid.Row="5" Content="Apply" MinWidth="80" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,8,8"/>
        </Grid>
    </ScrollViewer>
</Page>
